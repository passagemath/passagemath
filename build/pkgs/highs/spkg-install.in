cd src
mkdir -p build
cd build
sdh_cmake -GNinja \
          -DBUILD_SHARED_LIBS=ON \
          -DCMAKE_BUILD_TYPE=Release \
          -DBUILD_TESTING=OFF \
          -DBUILD_CXX=ON \
          -DZLIB=ON \
          -DCMAKE_VERBOSE_MAKEFILE=ON \
          -DCMAKE_INSTALL_LIBDIR=lib \
          ..
cmake --build .
DESTDIR=$SAGE_DESTDIR cmake --install .

sdh_generate_windows_lib_files highs
