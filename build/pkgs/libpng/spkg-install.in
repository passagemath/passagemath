# libpng needs to have no default options set for grep
unset GREP_OPTIONS

cd src

if [ -n "$MSYSTEM" ]; then
    autoreconf -fi
fi

sdh_configure --enable-shared --disable-static
sdh_make
sdh_make_install

sdh_generate_windows_lib_files png
