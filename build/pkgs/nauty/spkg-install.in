cd src

NAUTY_CONFIGURE=""

if [ "$SAGE_FAT_BINARY" = "yes" ]; then
   NAUTY_CONFIGURE+="--enable-generic --disable-popcnt"
fi

sdh_configure CC="$CC -fPIC" $NAUTY_CONFIGURE --includedir="$SAGE_LOCAL/include/nauty" --enable-static --disable-shared
sdh_make
sdh_make_install
