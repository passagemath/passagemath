[tox]
envlist = py27, py34, py35, py36, py37, py38, py39, py310, py311, py312, py313, py314
skip_missing_interpreters=true

[testenv]
setenv =
    # We make it harder to get the encoding right by using the dumbest default
    LC_ALL = C

commands={envpython} -m unittest discover
